# Services and Cron Automation

## systemctl

Used to manage background services:

- `systemctl start service`
- `systemctl stop service`
- `systemctl status service`
- `systemctl enable service`

## Cron Jobs

Automates scheduled tasks.

Format:
* * * * * command
